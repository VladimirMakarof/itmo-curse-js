объект не используется для хранения связи ключ - значение. объекты используются в ООП
в объект можно добавить только пару. 
что бы обратиться к свойству нужно 
1. название обекта (.) дальше свойство 
2. присвоить имя обекта 
свойствами могут быть строки, всё что к строке приобразуется но обычно это строки и тип символ(symbol).
Если мы пытаемся прочитать свойтва, которого нет в объекте. мы поулчаем undefined
// 1.что бы определить свойство нужно ставить null в случае если есть объект. но без значения (( а не undefined. после делаем перебор на null 

// 2. if ('githubLink' in userData) проверка с помощью оператора in
оператор in возрощает true если свойство с таким значением это имя свойства и false если не содержиться 
в случае если имя стостит из 2х слов то в кавычках и обрщение приосходит через квадратные скобки
for in это для объектов 

деструктуризация объект это получить отдельные переменные на основе этого объекта 